import * as React from 'react';
import classes from './AboutUs.module.scss';

type Props = {};

export function AboutUs({}: Props): React.ReactElement {
  return <div className={classes['wrapper']}>this is About us page</div>;
}
