import * as React from 'react';
import classes from './ContactUs.module.scss';

type Props = {};

export function ContactUs({}: Props): React.ReactElement {
  return <div className={classes['wrapper']}>this is contact us page</div>;
}
